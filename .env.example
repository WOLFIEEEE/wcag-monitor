# ============================================
# WCAG Monitor - Environment Configuration
# ============================================

# === REQUIRED SECRETS ===
# Generate secure random strings for these values
# You can use: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-at-least-32-characters
JWT_REFRESH_SECRET=change-this-to-another-secure-random-string-at-least-32-characters

# === DATABASE (Automatic - Internal Docker Network) ===
# MongoDB connection string - uses internal Docker network
DATABASE=mongodb://mongodb:27017/wcag-monitor

# === PUBLIC URLS ===
# Frontend URL - your main Vercel domain
FRONTEND_URL=https://your-domain.com

# Backend API URL (for Vercel frontend) - your Coolify backend domain
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# === CORS (Backend - Coolify) ===
# Comma-separated list of allowed origins for CORS
# Set to your Vercel frontend domain(s)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# === OPTIONAL: PAYMENT (Stripe) ===
# Leave empty if not using payments
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# === OPTIONAL: EMAIL (Resend) ===
# Leave empty if not using email notifications
RESEND_API_KEY=

# === APPLICATION SETTINGS ===
# Node environment
NODE_ENV=production

# Number of URLs free users can monitor
FREE_URL_LIMIT=2

# Cron schedule for automatic scans (default: daily at 12:30 AM)
# Format: second minute hour day month weekday
CRON=0 30 0 * * *

# Number of concurrent workers for scanning
NUM_WORKERS=2

# ============================================
# DEPLOYMENT NOTES (Split Architecture)
# ============================================
# Frontend (Vercel):
#   - Set NEXT_PUBLIC_API_URL in Vercel dashboard
#   - See VERCEL_SETUP.md for instructions
#
# Backend (Coolify):
#   - Set ALLOWED_ORIGINS to allow Vercel frontend
#   - Set all other variables in Coolify UI
#   - MongoDB data persists in Docker volumes
#   - See COOLIFY_SETUP.txt for instructions

